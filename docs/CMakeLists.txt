message(FATAL_ERROR Not currently configured)

# Roughly this, but not tested
# Needs to run one of the below programs with the asciidoc.conf file from this directory
find_program(A2X a2x REQUIRED)
find_program(ASCIIDOC asciidoc REQUIRED)
